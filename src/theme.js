import { createTheme } from '@mui/material/styles';


const myTheme = createTheme({
    palette: {
        type: 'light',
        primary: {
            main: '#124c17',
        },
        secondary: {
            main: '#ffab00',
        },
    },
});

export default myTheme;