import { createTheme } from '@mui/material/styles';


const myTheme = createTheme({
    palette: {
        type: 'light',
        primary: {
            main: '#ff9100',
        },
        secondary: {
            main: '#cf9efb',
        },
    },
});

export default myTheme;